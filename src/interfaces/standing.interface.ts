export interface Standing {
  ranking: number;
  teamName: string;
  matchesPlayed: number;
  points: number;
  goalsFor: number;
  goalsAgainst: number;
  lastResults: string;
  previousRanking: number;
}
